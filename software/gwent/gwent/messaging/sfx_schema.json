{
  "id": "https://gwent.shanaghy.com/sfx.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Sound effects message",
  "type": "object",
  "required": [
    "subkind"
  ],
  "properties": {
    "subkind": {
      "type": "string",
      "description": "Indicates the content of this message",
      "enum": [
        "announcement",
        "announcement_complete",
        "effect",
        "music"
      ]
    }
  },
  "oneOf": [
    { "$ref": "#/definitions/Announcement" },
    { "$ref": "#/definitions/AnnouncementComplete" },
    { "$ref": "#/definitions/Effect" },
    { "$ref": "#/definitions/Music" }
  ],
  "definitions": {
    "Announcement": {
      "type": "object",
      "required": [
        "announcement"
      ],
      "properties": {
        "subkind": { "const": "announcement" },
        "announcement": {
          "description": "The message to announce via TTS",
          "type": "string"
        }
      }
    },
    "AnnouncementComplete": {
      "type": "object",
      "required": [
        "announcement"
      ],
      "properties": {
        "subkind": { "const": "announcement_complete" },
        "announcement": {
          "description": "The announcement that completed",
          "type": "string"
        }
      }
    },
    "Effect": {
      "type": "object",
      "required": [
        "effect"
      ],
      "properties": {
        "effect": {
          "description": "The sound effect to play",
          "type": "string"
        }
      }
    },
    "Music": {
      "type": "object",
      "required": [
        "random"
      ],
      "properties": {
        "music": {
          "description": "The music file to play in the background",
          "type": "string"
        },
        "random": {
          "description": "Plays a random background track",
          "type": "boolean"
        }
      }
    }
  }
}
